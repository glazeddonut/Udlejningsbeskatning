node_modules
dist
# DB-filen indeholder personlige data (CPR m.m.) og committes ALDRIG
udlejning-data.json
# Uploadede bilag (kvitteringer) — personlige, committes ALDRIG
bilag/
*.local
.DS_Store
# Lokale Claude Code-tilladelser (maskinspecifikke)
.claude/settings.local.json
# Lokal docker-compose (kopiér fra docker-compose.yml.example)
docker-compose.yml
